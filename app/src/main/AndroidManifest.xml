<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.CareCircle.myapp">

    <!-- Permissions required by the app -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- for Google Maps, network -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- precise location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- approximate location -->
    <uses-permission android:name="android.permission.SEND_SMS" /> <!-- send emergency SMS -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- optional for telephony features -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" /> <!-- optional for incoming SMS handling -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" /> <!-- fingerprint login -->

    <!-- Feature declaration (optional, telephony not required for devices without SIM) -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Care Circle"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDWGm-BMdLUH5fohNfNChN-NB-9jOO72Fg" />

        <!-- Splash screen as launcher activity -->
        <activity
            android:name=".Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All other activities in the app -->
        <activity android:name=".Login" />
        <activity android:name=".Signup" />
        <activity android:name=".Home" />
        <activity android:name=".AboutUs" />
        <activity android:name=".CareCircleList" />
        <activity android:name=".AddContact" />
        <activity android:name=".MapScreen" />
        <activity android:name=".AlertHistory" />
        <activity android:name=".LocalEmergencyNumbers" />

    </application>
</manifest>
